name: internationalization
description: A new Flutter project.
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  bloc: ^8.1.0
  cupertino_icons: ^1.0.5
  flutter_bloc: ^8.1.1
  intl: ^0.17.0
  equatable: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  # Essential for the localization packages’ automatic code generation.
  generate: true
  
  uses-material-design: true
